{
  "name": "llm-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Diagnosis + remediation pipeline for the NEXT_HARNESS agent: reads harness_agent_runs, classifies failures, and dispatches fixes into NEXT_HARNESS.",
  "type": "module",
  "scripts": {
    "report": "tsx scripts/report.ts",
    "remediate": "tsx scripts/remediate.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.2",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
